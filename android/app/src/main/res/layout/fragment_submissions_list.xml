<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background">

    <!-- ============================================ -->
    <!-- TOOLBAR + FILTER HEADER -->
    <!-- ============================================ -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/primary"
        android:elevation="2dp"
        android:paddingBottom="@dimen/space_sm">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:background="@color/primary"
            android:theme="@style/ThemeOverlay.MaterialComponents.Dark.ActionBar"
            app:title="@string/title_saved_submissions"
            app:navigationIcon="@drawable/ic_arrow_back"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/space_md"
            android:layout_marginTop="@dimen/space_xs"
            android:text="@string/subtitle_submissions"
            android:textAppearance="@style/TextAppearance.Sync.BodySmall"/>

        <!-- Filter chips -->
        <com.google.android.material.chip.ChipGroup
            android:id="@+id/chipGroupFilter"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/space_md"
            android:layout_marginEnd="@dimen/space_md"
            android:layout_marginTop="@dimen/space_sm"
            app:singleSelection="true"
            app:selectionRequired="true"
            app:chipSpacingHorizontal="@dimen/space_sm">

            <com.google.android.material.chip.Chip
                android:id="@+id/chipAll"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:checked="true"
                android:text="@string/filter_all"
                android:textColor="@color/white"
                app:chipBackgroundColor="@color/primary"/>

            <com.google.android.material.chip.Chip
                android:id="@+id/chipPending"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/filter_pending"
                android:textColor="@color/white"
                app:chipBackgroundColor="@color/primary"/>

            <com.google.android.material.chip.Chip
                android:id="@+id/chipSyncing"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/filter_syncing"
                android:textColor="@color/white"
                app:chipBackgroundColor="@color/primary"/>

            <com.google.android.material.chip.Chip
                android:id="@+id/chipSynced"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/filter_synced"
                android:textColor="@color/white"
                app:chipBackgroundColor="@color/primary"/>

            <com.google.android.material.chip.Chip
                android:id="@+id/chipFailed"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/filter_failed"
                android:textColor="@color/white"
                app:chipBackgroundColor="@color/primary"/>

        </com.google.android.material.chip.ChipGroup>

    </LinearLayout>

    <!-- ============================================ -->
    <!-- LOADING -->
    <!-- ============================================ -->

    <LinearLayout
        android:id="@+id/loadingContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone"
        android:paddingTop="48dp">

        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:indeterminateTint="@color/primary"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_md"
            android:text="Loading submissions..."
            android:textAppearance="@style/TextAppearance.Sync.BodySmall"/>

    </LinearLayout>

    <!-- ============================================ -->
    <!-- LIST -->
    <!-- ============================================ -->

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefresh"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvSubmissions"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingTop="@dimen/space_sm"
            android:paddingBottom="@dimen/space_md"
            android:visibility="gone"
            android:scrollbars="vertical"
            tools:listitem="@layout/item_submission" />

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!-- ============================================ -->
    <!-- EMPTY STATE -->
    <!-- ============================================ -->

    <LinearLayout
        android:id="@+id/containerEmpty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone"
        android:paddingHorizontal="@dimen/space_md">

        <ImageView
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:src="@drawable/ic_inbox_empty"
            android:importantForAccessibility="no"/>

        <TextView
            android:id="@+id/tvEmpty"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_md"
            android:gravity="center"
            android:text="@string/msg_no_submissions"
            android:textColor="@color/secondary_text"
            android:textAppearance="@style/TextAppearance.Sync.Body"/>

    </LinearLayout>

    <!-- ============================================ -->
    <!-- BOTTOM ACTIONS -->
    <!-- ============================================ -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="@dimen/space_md"
        android:background="@color/primary"
        android:elevation="4dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSyncAll"
            style="@style/Widget.Sync.Button.Filled"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="@dimen/space_sm"
            android:text="@string/btn_sync_all_pending"
            app:icon="@drawable/ic_sync"
            app:iconGravity="textStart"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnViewForms"
            style="@style/Widget.Sync.Button.Outlined"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/btn_view_forms"/>

    </LinearLayout>

</LinearLayout>