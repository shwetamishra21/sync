<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background">

    <!-- ============================================ -->
    <!-- TOOLBAR -->
    <!-- ============================================ -->

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="@color/primary"
        android:theme="@style/ThemeOverlay.MaterialComponents.Dark.ActionBar"
        android:elevation="2dp"
        app:title="@string/title_submission_details"
        app:navigationIcon="@drawable/ic_arrow_back"/>

    <!-- ============================================ -->
    <!-- LOADING STATE -->
    <!-- ============================================ -->

    <LinearLayout
        android:id="@+id/loadingContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone"
        android:paddingTop="48dp">

        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:indeterminateTint="@color/primary"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_md"
            android:text="Loading submission..."
            android:textAppearance="@style/TextAppearance.Sync.BodySmall"/>

    </LinearLayout>

    <!-- ============================================ -->
    <!-- CONTENT -->
    <!-- ============================================ -->

    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="@dimen/space_md"
            android:paddingTop="@dimen/space_md"
            android:paddingBottom="@dimen/space_lg">

            <!-- ============================================ -->
            <!-- METADATA SECTION -->
            <!-- ============================================ -->

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingBottom="@dimen/space_md">

                <!-- Form Name -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/label_form"
                    android:textAppearance="@style/TextAppearance.Sync.BodySmall"
                    android:paddingBottom="@dimen/space_xs"/>

                <TextView
                    android:id="@+id/tvFormName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textAppearance="@style/TextAppearance.Sync.Title"
                    tools:text="Contact Form"/>

                <!-- Submission ID -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/space_md"
                    android:text="@string/label_submission_id"
                    android:textAppearance="@style/TextAppearance.Sync.BodySmall"
                    android:paddingBottom="@dimen/space_xs"/>

                <TextView
                    android:id="@+id/tvFormId"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textAppearance="@style/TextAppearance.Sync.Body"
                    tools:text="Submission #12345"/>

                <!-- Created Date -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/space_md"
                    android:text="@string/label_created"
                    android:textAppearance="@style/TextAppearance.Sync.BodySmall"
                    android:paddingBottom="@dimen/space_xs"/>

                <TextView
                    android:id="@+id/tvCreatedAt"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textAppearance="@style/TextAppearance.Sync.Body"
                    tools:text="Jun 10, 2026 14:30"/>

            </LinearLayout>

            <!-- Divider -->
            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/divider"
                android:layout_marginVertical="@dimen/space_md"/>

            <!-- ============================================ -->
            <!-- SYNC STATUS SECTION -->
            <!-- ============================================ -->

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingVertical="@dimen/space_md">

                <!-- Status Chip -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/label_status"
                    android:textAppearance="@style/TextAppearance.Sync.BodySmall"
                    android:paddingBottom="@dimen/space_xs"/>

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipStatus"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:clickable="false"
                    android:focusable="false"
                    android:textColor="@color/white"
                    app:chipBackgroundColor="@color/primary"
                    tools:text="Pending"/>

                <!-- Synced Date (only visible when synced) -->
                <TextView
                    android:id="@+id/tvSyncedAt"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/space_md"
                    android:textAppearance="@style/TextAppearance.Sync.BodySmall"
                    android:visibility="gone"
                    tools:text="Synced: Jun 10, 2026 14:35"
                    tools:visibility="visible"/>

                <!-- Retry Count (only visible when > 0) -->
                <TextView
                    android:id="@+id/tvRetryCount"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/space_md"
                    android:textAppearance="@style/TextAppearance.Sync.BodySmall"
                    android:visibility="gone"
                    tools:text="Retries: 2"
                    tools:visibility="visible"/>

                <!-- Error Message (only visible on error) -->
                <TextView
                    android:id="@+id/tvErrorMessage"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/space_md"
                    android:padding="@dimen/space_md"
                    android:background="@drawable/bg_error_box"
                    android:textColor="@color/error"
                    android:textAppearance="@style/TextAppearance.Sync.BodySmall"
                    android:visibility="gone"
                    tools:text="Network error occurred"
                    tools:visibility="visible"/>

            </LinearLayout>

            <!-- Divider -->
            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/divider"
                android:layout_marginVertical="@dimen/space_md"/>

            <!-- ============================================ -->
            <!-- FORM DATA SECTION -->
            <!-- ============================================ -->

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/label_form_data"
                android:textAppearance="@style/TextAppearance.Sync.Title"
                android:paddingBottom="@dimen/space_md"/>

            <LinearLayout
                android:id="@+id/containerFormData"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"/>

            <!-- ============================================ -->
            <!-- ACTION BUTTONS -->
            <!-- ============================================ -->

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center"
                android:layout_marginTop="@dimen/space_lg">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnSync"
                    style="@style/Widget.Sync.Button.Filled"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginEnd="@dimen/space_sm"
                    android:text="@string/btn_sync_now"
                    app:icon="@drawable/ic_sync"
                    app:iconGravity="textStart"/>

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnDelete"
                    style="@style/Widget.Sync.Button.Outlined"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@string/btn_delete"
                    app:icon="@drawable/ic_delete"
                    app:iconGravity="textStart"/>

            </LinearLayout>

        </LinearLayout>

    </ScrollView>

</LinearLayout>